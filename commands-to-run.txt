python -m venv venv

venv\Scripts\activate          # cmd.exe    or
.\venv\Scripts\Activate.ps1    # PowerShell (may need: Set-ExecutionPolicy -Scope Process RemoteSigned)   or
source venv/Scripts/activate   # Git Bash

pip install -r backend/requirements.txt
pip install -r NiceGUI/requirements.txt

# Terminal 1
cd backend
setx GROQ_API_KEY "paste grok api"
set BASEERA_CPU_THREADS=2
set BASEERA_LLM_MODEL=Qwen/Qwen2.5-0.5B-Instruct
set BASEERA_WHISPER_MODEL=tiny
uvicorn main:app --port 8000

# Terminal 2 (same venv activated again, new terminal)
cd NiceGUI
python app.py